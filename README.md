# 🧠 NeuralCrypt
![C++](https://img.shields.io/badge/Language-C++20-blue.svg)
![Status](https://img.shields.io/badge/Status-Experimental-orange.svg)

> [!WARNING]
> ### This is just PoC
> #### don't use this in production

**NeuralCrypt** — это экспериментальная header-only C++ библиотека для **обфускации указателей**. 

Она использует архитектуру **сети Фейстеля (Feistel Cipher)**, ядром которой является полносвязная **нейронная сеть**. Ключи шифрования генерируются на этапе компиляции (`Compile-Time`).

## ✨ Особенности

*   **Neural Core**: В качестве F-функции шифрования используется легковесный перцептрон с весами, которые в теории можно "натренировать" под свой проект.
*   **Compile-Time Keys**:  Ключи генерируются макросами на этапе компиляции.
*   **Shit-Anti-Optimization**: Барьеры памяти предотвращает удаление переменных оптимизатором (`-O2`/`-O3`).
*   **Example Secure Singleton**: Реализация паттерна Singleton, где инстанс класса всегда хранится в зашифрованном виде.
---

## 🛠 Использование
### 1. Защита обычных указателей
Макрос `CRYPT_PTR`. Он создает "умный" объект, который хранит адрес в зашифрованном виде и расшифровывает его в момент обращения.

```cpp
#include "neural_obfuscator.hpp"
#include <iostream>

int main() {
    int secret_value = 1337;
    
    // Адрес переменной шифруется уникальным ключом, зависящим от строки кода
    auto secure_ptr = CRYPT_PTR(&secret_value);

    // При обращении происходит мгновенная дешифровка -> доступ -> зачистка регистров
    std::cout << "Secret: " << *secure_ptr << std::endl; 
    
    *secure_ptr = 9999; // Запись тоже работает прозрачно
    
    return 0;
}
```
За идею и вдохновение спасибо: https://github.com/sapdragon/ai-obfuscator
